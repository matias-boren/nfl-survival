{
  "buildCommand": "echo 'Using pre-built Flutter web app'",
  "outputDirectory": "build/web",
  "installCommand": "echo 'No installation needed for pre-built app'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}